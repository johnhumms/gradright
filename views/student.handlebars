{{!-- student options --}}
<ul>
    {{#each students}}
    <li>
        <a href="/student/{{id}}">{{student_name}} </a>
        | {{school_name}} | {{current_year}}th Grade | {{district.district_name}}
    </li>
    {{/each}}
</ul>


<a href="/add-student"><button class="btn btn-info" id="add-stu">Add Student</button></a>
